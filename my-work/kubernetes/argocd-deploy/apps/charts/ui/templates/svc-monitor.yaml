# service-monitor.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: {{ include "ui.fullname" . }}-svc-mon

  labels:
    release: kube-prometheus-stack

spec:
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace }}

  selector:
    matchLabels:
      {{- include "ui.selectorLabels" . | nindent 6 }}

  endpoints:
  - port: 8080
    path: /actuator/prometheus
    interval: 15s
    
    relabelings:
    - targetLabel: env
      replacement: {{ .Release.Namespace }}